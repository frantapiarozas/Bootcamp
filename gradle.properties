## For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
#
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx1024m -XX:MaxPermSize=256m
# org.gradle.jvmargs=-Xmx2048m -XX:MaxPermSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
#
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. For more details, visit
# https://developer.android.com/r/tools/gradle-multi-project-decoupled-projects
# org.gradle.parallel=true
#Wed Aug 12 10:33:15 CLT 2026
android.useAndroidX=true
kotlin.code.style=official
org.gradle.configuration-cache=false
org.gradle.jvmargs=-Xmx1024M -Dkotlin.daemon.jvm.options\="-Xmx1024M" -Dfile.encoding\=UTF-8
# limita el heap de Gradle
org.gradle.jvmargs=-Xmx1024m -XX:MaxMetaspaceSize=512m
# desactiva la ejecucion en paralelo si tienes menos de 16 GB de RAM
org.gradle.parallel=false
# desactiva el almacenamiento en cache agresivo si no es necesario
org.gradle.caching=false